<%= form_for(@twitt) do |f| %>
  <% if @twitt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twitt.errors.count, "error") %> prohibited this twitt from being saved:</h2>

      <ul>
      <% @twitt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :post %><br />
    <%= f.text_area :post %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
